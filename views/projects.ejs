<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Our Projects | Wonta2777 Foundation</title>
  <link rel="stylesheet" href="/style.css" />
</head>
<body>
  <!-- Header -->
  <header>
    <div class="logo">
      <h1>Wonta2777 Foundation</h1>
    </div>
    <button class="menu-toggle" aria-label="Toggle menu">☰</button>
    <nav id="navbar">
      <a href="/" class="<%= page === 'home' ? 'active' : '' %>">Home</a>
      <a href="/projects" class="<%= page === 'projects' ? 'active' : '' %>">Projects</a>
      <a href="/about" class="<%= page === 'about' ? 'active' : '' %>">About</a>
      <a href="/contact" class="<%= page === 'contact' ? 'active' : '' %>">Contact</a>
    </nav>
  </header>

  <!-- Main Content -->
  <main>
    <!-- Projects Section -->
    <section>
      <h2>Our Projects</h2>
      <p>
        At Wonta2777 Foundation, we are committed to sustainable rural development
        through impactful programs in <strong>education, healthcare, agriculture, and
        women’s empowerment</strong>. Each project is a step toward building
        self-reliant, thriving communities.
      </p>
    </section>

    <!-- Project Cards -->
    <section class="projects-grid">
      <% projects.forEach(project => { %>
        <div class="project-card">
          <img src="<%= project.image %>" alt="<%= project.title %>">
          <h3><%= project.title %></h3>
          <h4><%= project.category %></h4>
          <p><%= project.description %></p>
          <p><strong>Impact:</strong> <%= project.impact %></p>
          <a href="<%= project.link %>" class="btn">Learn More</a>
        </div>
      <% }) %>
    </section>

    <!-- Impact Summary -->
    <section class="impact-section">
      <h2>Our Impact</h2>
      <div class="impact-grid">
        <div>
          <h3>🏫 20+</h3>
          <p>Schools supported</p>
        </div>
        <div>
          <h3>💉 15,000+</h3>
          <p>People reached with healthcare</p>
        </div>
        <div>
          <h3>🌾 4,500+</h3>
          <p>Farmers trained</p>
        </div>
        <div>
          <h3>👩‍🌾 600+</h3>
          <p>Women empowered</p>
        </div>
      </div>
    </section>

    <!-- Call to Action -->
    <section class="cta-section">
      <h2>Join Our Mission</h2>
      <p>
        Together, we can continue to bring education, health, and sustainable livelihoods
        to rural communities. Your support makes lasting change possible.
      </p>
      <a href="/donate" class="btn">Get Involved</a>
    </section>
  </main>

  <!-- Footer -->
  <footer>
    <p>© <%= new Date().getFullYear() %> Wonta2777 Foundation | Empowering Rural Communities 💚</p>
  </footer>

  <!-- JS for mobile menu -->
  <script>
    const toggle = document.querySelector('.menu-toggle');
    const navbar = document.getElementById('navbar');
    toggle.addEventListener('click', () => navbar.classList.toggle('active'));
  </script>
</body>
</html>
