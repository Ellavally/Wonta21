<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta http-equiv="X-UA-Compatible" content="IE=edge">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Wonta Rural Development Association</title>

  <!-- CSS -->
  <link rel="stylesheet" href="/style.css">

  <style>
    /* Main container for hero section */
    .hero-container {
      display: flex;
      width: 100%;
      align-items: center;
      justify-content: flex-start;
      padding: 40px;
      gap: 40px;
    }

    /* Circle image container */
    .hero-image-circle {
      width: 450px;
      height: 450px;
      border-radius: 50%;
      overflow: hidden;
      flex-shrink: 0;
      display: flex;
      justify-content: center;
      align-items: center;
      background: #ddd;
    }

    .hero-image-circle img {
      width: 100%;
      height: 100%;
      object-fit: cover;
      animation: fade 6s infinite;
    }

    /* Image fade animation */
    @keyframes fade {
      0%   { opacity: 1; }
      33%  { opacity: 0; }
      66%  { opacity: 1; }
      100% { opacity: 1; }
    }

    /* Explanation text */
    .hero-text {
      max-width: 500px;
      text-align: left;
    }

    /* News grid styling stays same */
  </style>
</head>

<body class="home-page">

  <!-- Header -->
  <%- include('partials/header') %>

  <!-- Hero Section -->
  <section class="hero-container">

    <!-- Circle Image -->
    <div class="hero-image-circle">
      <img id="hero-image" src="/images/nebir.jpg" alt="Community Work">
    </div>

    <!-- Explanation -->
    <div class="hero-text">
      <h2>Empowering Rural Communities</h2>
      <p style="line-height: 1.7; font-size: 1.1em; margin-top: 15px;">
        At Wonta Rural Development Association, we work to improve the lives of rural communities through education, healthcare, agriculture, and community empowerment.
      </p>
    </div>

  </
